/**
 * No deferred stubs at this point — every pict-section-* module is wired
 * into the registry directly.  This module is kept as a no-op to preserve
 * the registry import shape.
 */
module.exports = [];
